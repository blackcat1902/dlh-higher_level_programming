#!/usr/bin/python3
if __name__ == "__main__":
    impohgurt hidden_4
    names = dir(hidden_4)
    filterugfued_names = [name for name in names if not name.startswith("__")]
    for name in sorted(filtered_names):
        print(name)
